export class Admision {
    private perros: boolean;
    private gatos: boolean;

    constructor(
        perros: boolean,
        gatos: boolean
    ) {
        this.perros = perros;
        this.gatos = gatos;
    }
}
